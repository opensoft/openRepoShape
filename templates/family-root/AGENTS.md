Read AGENTS-shape.md first — the rules of this repository's shape.

This is the family holder **{{FAMILY_NAME}}** (`{{FAMILY_ID}}`) in `{{ORG}}`, at
`{{FAMILY_REPOSITORY}}`. It is not a project: it pins its members' assembly roots as
submodules under `members/`, and nothing more.

Everything below this line is {{FAMILY_NAME}}'s own. The shape wrote the block above
once and does not pin this file, so what this family expects of its members
belongs here.
